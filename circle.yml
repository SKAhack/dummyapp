machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"

  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
    - docker build -t skahack/dummyapp:$CIRCLE_SHA1 .
    - mkdir -p ~/docker; docker save skahack/dummyapp > ~/docker/image.tar

test:
  override:
    - docker run -d -p 3030:3030 skahack/dummyapp:$CIRCLE_SHA1; sleep 5
    - curl --retry 10 --retry-delay 5 -v http://localhost:3030

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push skahack/dummyapp:$CIRCLE_SHA1
